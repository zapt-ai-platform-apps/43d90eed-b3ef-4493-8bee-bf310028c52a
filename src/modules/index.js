/**
 * Main modules index file
 * Used to initialize all modules when the application starts
 */

/**
 * Initializes all modules
 * This is called once when the app starts
 */
export async function initializeModules() {
  // Currently no initialization is needed
  // Future module initialization can be added here as needed
  console.log('Modules initialized');
}